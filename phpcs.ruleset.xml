<?xml version="1.0"?>
<ruleset name="Custom">
    <!-- Enable colors in report -->
    <arg name="colors"/>

    <!-- Add source codes in the report -->
    <arg value="s"/>

    <!-- Load WordPressVIPMinimum Coding standards -->
    <rule ref="WordPressVIPMinimum"/>
    <rule ref="WordPressVIPMinimum.VIP.RestrictedFunctions">
        <exclude name="WordPressVIPMinimum.VIP.RestrictedFunctions.wp_is_mobile_wp_is_mobile"/>
        <exclude name="WordPressVIPMinimum.VIP.RestrictedFunctions.term_exists_term_exists"/>
    </rule>
</ruleset>
